export const postMediaUrl = (url, memoryId) => {
    // TODO: send url to backend
    console.log("sending media url", url)
}

export const getMediaUrl = (mediaId) => {
    // TODO: get url from backend
    console.log("fetching media url for", mediaId)
}
